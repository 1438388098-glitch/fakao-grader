# fakao-grader 设计文档

日期：2026-09-14 ｜ 状态：已按此实现（v1）

## 目标

为法考主观题备考（距 2026 主观题考试约 33 天）提供"做题 → 按采分点严格批改 → 深度复盘 → 成绩追踪"的本地闭环。范围 v1：本地题库 34 道真题（民综在民法/民诉下重复出现，按一次计）。

## 决策记录

| 决策点 | 结论 | 依据 |
|---|---|---|
| 输入方式 | 本地文件作答（作答\科目-年份.md），粘贴兜底 | 用户选择；长答案可编辑、天然留档 |
| 判分机制 | 按采分点严格判定，✓/△/✗，不倒扣 | 用户选择；与法考按点给分口径一致 |
| 报告 | 深度复盘版 | 用户选择 |
| 范围 | 仅本地 34 题，自定义题留作扩展 | 用户选择 |
| 架构 | SKILL.md + extract.js 辅助脚本，判分由模型语义判定 | 方案 B；抽题可靠、省上下文，智能留给模型 |
| 程序初评 | 否 | 中文同义表述多，机械匹配误判率高 |

## 调研修正（2026-09-14 网络调研）

1. 真实阅卷"先定档、档内采点"→ 报告输出双分数：采分点得分（训练口径）+ 考场预估带（±3 分定档折算）
2. 三段论为公认给分框架 → 采分点标注类型（结论/依据/分析），"结论对理由不全"自然分解为不同点的 ✓/✗，减少模糊 △
3. 定性错误不倒扣但问内连锁失分 → 报告显式画连锁失分链
4. LLM 评分波动是行业痛点 → 校准样例 + 置信度标注 + 中低置信二次复核 + ±3 分波动声明
5. 选做题（商法/行政二选一）双答只计第一道 → 报告提示

## 关键数据事实

- 题库 JSON 中 `subKeyWord` 字段是平台结构化评分标准：每点含 text/score/whiteList（等义白名单）/blackList（矛盾黑名单）
- 234 个小问中 191 个有 subKeyWord，**每问采分点分值之和精确等于问分值**（含 0.5 分细粒度）
- 43 个小问无结构化点 → 判卷时按参考答案自行拆点均分

## 组件

- `SKILL.md`：触发条件、流程、判分规则、校准样例、报告模板、边界情况
- `scripts/extract.js`：`node extract.js <科目别名> [年份]` → 输出评分作业单（Markdown/stdout）；无年份列题目；环境变量 `FAKAO_DATA_DIR` 可覆盖数据目录
- 工作区目录：`作答\`（用户作答）、`批改记录\`（报告存档 + 成绩追踪.csv）

## 已知限制 / 后续扩展

- 2022 年刑法题平台分值为 0（数据源缺失），批改时以采分点总和为准并在报告注明
- 自定义题（用户自带评分标准）模式未实现
- 纯程序化预判未实现（有意为之，见决策记录）
